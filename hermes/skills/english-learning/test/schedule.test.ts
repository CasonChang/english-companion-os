import {describe,expect,it,vi} from "vitest";import {claimReviewSchedule,scheduleMessage,updateReviewSettings} from "../src/schedule.js";
describe("review scheduling",()=>{it("claims through the atomic RPC",async()=>{const rpc=vi.fn().mockResolvedValue({data:{action:"review",question_count:4},error:null});expect((await claimReviewSchedule({rpc} as never,"u")).action).toBe("review")});it("formats only actionable claims",()=>{expect(scheduleMessage({action:"silent"})).toBeNull();expect(scheduleMessage({action:"nudge"})).toContain("練英文")});it("updates settings without inventing omitted values",async()=>{const rpc=vi.fn().mockResolvedValue({data:{question_count:5},error:null});await updateReviewSettings({rpc} as never,"u",{questionCount:5});expect(rpc).toHaveBeenCalledWith("update_telegram_review_settings",expect.objectContaining({p_question_count:5,p_timezone:null}))})});
